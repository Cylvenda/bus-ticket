import {Card, CardContent, CardHeader, CardTitle} from "@/components/ui/card.tsx";

const RouteFound = () => {
    return (
        <div className="bg-primary max-w-5xl mx-auto w-full">
            <Card>
                <CardHeader>
                    <CardTitle className="text-xl">Arusha to Morogoro</CardTitle>
                </CardHeader>

                <CardContent>

                </CardContent>
            </Card>
        </div>
    );
};

export default RouteFound;